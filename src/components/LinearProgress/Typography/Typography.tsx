import { Typography } from "@mui/material";
import React from "react";

const ReactTypography = (props: any) => {
  return (
    <Typography {...props}>
    </Typography>
  );
};

export default ReactTypography;