import { AccordionSummary } from "@mui/material";
import React from "react";

const ReactAccordionSummary = (props: any) => {

    return (
            <AccordionSummary
                {...props}
            >
            </AccordionSummary>
    );
};

export default ReactAccordionSummary
