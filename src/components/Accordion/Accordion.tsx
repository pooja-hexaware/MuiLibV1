import { Accordion } from "@mui/material";
import React from "react";

const ReactAccordion = (props: any) => {

    return (
            <Accordion
                {...props}
            >
            </Accordion>
    );
};

export default ReactAccordion
