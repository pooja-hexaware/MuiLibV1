import { AccordionDetails } from "@mui/material";
import React from "react";

const ReactAccordionDetails = (props: any) => {

    return (
            <AccordionDetails
                {...props}
            >
            </AccordionDetails>
    );
};

export default ReactAccordionDetails
