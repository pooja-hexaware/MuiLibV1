import React from 'react';
import { AlertTitle } from '@mui/material';

const ReactAlertTitle = (props: any) => {
    return (
            <AlertTitle {...props} />
    );
};

export default ReactAlertTitle;