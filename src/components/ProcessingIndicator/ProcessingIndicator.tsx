import * as React from "react";
import CircularProgress from "@mui/material/CircularProgress";

const ProcessingIndicator = (props: any) => {
  return <CircularProgress {...props} />;
};

export default ProcessingIndicator;
